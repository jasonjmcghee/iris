<%= form_for(@ownership) do |f| %>
  <% if @ownership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ownership.errors.count, "error") %> prohibited this ownership from being saved:</h2>

      <ul>
      <% @ownership.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_email %><br>
    <%= f.select :user_email, options_from_collection_for_select(User.all, :email, :email) %>
  </div>
  <div class="field">
    <%= f.label :device_name %><br>
    <%= f.select :device_name, options_from_collection_for_select(Device.all, :name, :name) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
