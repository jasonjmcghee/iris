<%= form_for(@user_mac) do |f| %>
  <% if @user_mac.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_mac.errors.count, "error") %> prohibited this user_mac from being saved:</h2>

      <ul>
      <% @user_mac.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.select :user_id, options_from_collection_for_select(User.all, :id, :login) %>
  </div>
  <div class="field">
    <%= f.label :mac_address_id %><br>
    <%= f.select :mac_address_id, options_from_collection_for_select(MacAddress.all, :id, :address) %>
  </div>

  <div class="actions">
    <%= f.submit "Add Mac Adress" %>
  </div>
<% end %>
